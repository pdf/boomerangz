version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - exhaustive
    - govet
    - nilerr
    - noctx
    - revive
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
